import { Box, Heading, Text } from "@chakra-ui/react";

const About = () => (
  <Box p={4}>
    <Heading>About Us</Heading>
    <Text mt={4}>
      This is a sample web application to demonstrate CRUD operations and navigation in a React app.
    </Text>
  </Box>
);

export default About;